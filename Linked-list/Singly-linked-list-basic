#include <stdio.h>
#include <stdlib.h>
struct node {
  int value;
  struct node *next;
};
int main() {
  struct node *one = NULL;
  one = malloc(sizeof(struct node));
  one->value = NULL;
  one->next = 10;
  printf("%d ", one->value);
  printf("%d ", one->next);
}
